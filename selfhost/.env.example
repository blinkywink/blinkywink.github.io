# Copy to .env on the N100. Never commit .env.

POSTGRES_PASSWORD=change-me-long-random
# openssl rand -hex 32
JWT_SECRET=
# node scripts/mint-anon-jwt.mjs  (uses JWT_SECRET)
PGRST_ANON_JWT=

# Cloudflare Zero Trust → Networks → Tunnels → create → copy token
CLOUDFLARE_TUNNEL_TOKEN=

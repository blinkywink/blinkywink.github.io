# SPA fallback (Cloudflare Pages serves real files first).
# Installers: functions/downloads/[file].ts 302s to GitHub's blob CDN
# (do not add a /downloads redirect here — it would run first and send
# people to github.com Sign in).
/*    /index.html   200

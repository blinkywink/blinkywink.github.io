-- award_cards + award_coins with session auth and anti-mint rate limits.
-- Canonical source: supabase/reward_rate_limits.sql
-- Do not redefine award_cards here - re-run reward_rate_limits.sql instead.

-- (Intentionally empty besides this note. Kept so older docs that mention
--  fix_award_cards_session.sql still point at a real file.)
